import 'package:flutter/material.dart';

class BottomChallenges extends StatelessWidget {
  const BottomChallenges({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue,
    );
  }
}
